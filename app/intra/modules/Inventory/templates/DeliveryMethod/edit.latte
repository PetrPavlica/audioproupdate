{block title}{_'Editace dodací metody'}{/block}
{block content}

<h3 class="page-header">
    {_'Editace dodací metody'}
</h3>

<div style="" class="col-xs-12 col-sm-12 col-md-8">
    {form form, class=>"form-horizontal"}
        {control renderer $form, 'id'}
        {control renderer $form, 'name'}
        {control renderer $form, 'heurekaCode'}
        {control renderer $form, 'selingPrice'}
        {control renderer $form, 'days'}
        {control renderer $form, 'vat'}
        {control renderer $form, 'section'}
        {control renderer $form, 'balikobotDelivery'}
        {control renderer $form, 'heurekaDelivery'}
        {control renderer $form, 'heurekaStore'}
        {control renderer $form, 'active'}

        {control renderer $form, 'isDPDClassic'}
        {control renderer $form, 'isDPD'}
        {control renderer $form, 'isUlozenka'}
        {control renderer $form, 'isZasilkovna'}

        {control renderer $form, 'bubbleInfo'}
        {control renderer $form, 'info'}
        {control renderer $form, 'methodImg'}
        {snippet method-img}
            {ifset $method}
                <div n:if="$method->image" class="col-md-6 col-md-offset-4" style="padding-bottom: 20px">
                    <img src="{$basePath}/{$method->image}"/>
                    <a n:href="deleteImg! methodId => $method->id" class="btn btn-xs btn-danger"
                                                                   data-confirm="modal"
                                                                   data-ajax="on">
                        <span class="fa fa-trash"></span>
                    </a>
                </div>
            {/ifset}
        {/snippet}
        {control renderer $form, 'send'}
    {/form}
    <a n:href=":DeliveryMethod:default" class="btn btn-default">
        <span class="fa fa-arrow-left"></span>
        {_'Zpět'}
    </a>
</div>